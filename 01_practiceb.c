# include<stdio.h>

int main(){
    int length;
    int breadth;
    //int area = length * breadth;
    printf(" What is the length of ractangle\n");
    scanf("%d", &length);

    printf("What is the breadth of ractangle\n");
    scanf("%d", &breadth);

    printf("The area of your ractangle is %d", length* breadth);


    return 0;
}
